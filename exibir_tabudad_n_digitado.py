tabuada = (int(input("Digite o número de sua preferência: ")))
quantidade = 0

while quantidade < 10:
    #quantidade = quantidade +1
    quantidade += 1

    print (f"{tabuada} X {quantidade} = {quantidade * tabuada}")